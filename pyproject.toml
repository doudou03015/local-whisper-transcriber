[project]
name = "local-whisper-transcriber"
version = "0.2.0"
description = "A Windows GUI app for local faster-whisper transcription."
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
